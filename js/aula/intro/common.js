jQuery(function(){
    // $("p:even").css("backgroundColor", "red")
    // $("p:odd").css("backgroundColor", "red")

    // Pegando Valor do campo
    $("input[type=text]").val();

    // Mudando valor do campo
    $("input[type=text]").val("Novo");

    // O mesmo vale p/ a func. html
    // $("input").html("<p>Novo</p>")
    // $("input").html()
    
});

/*
* $(document).ready(function(){

// Código executado qdo a pag. é carregada
// ou seja, qdo o DOM está pronto
$(".ingredientes").slideUp();

});
*/
